{
  "name": "frontend",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "dev": "vite --mode development",
    "build": "vite build --mode production",
    "deploy": "npm run build && aws s3 rm s3://www.solanagateway.com/ --recursive && aws s3 cp ./dist s3://www.solanagateway.com/ --recursive && aws cloudfront create-invalidation --distribution-id EU9T8S2HTIFNQ --paths \"/*\""
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.4.3",
    "vite": "^2.5.7",
    "vite-plugin-solid": "^2.0.3"
  },
  "dependencies": {
    "date-fns": "^2.27.0",
    "solid-app-router": "^0.1.13",
    "solid-js": "^1.1.3",
    "solid-styled-components": "^0.27.3",
    "solid-transition-group": "0.0.8"
  }
}
