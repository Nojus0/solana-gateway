{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "install": "yarn build",
        "build": "cd ./packages/shared && tsc && cd ../api && tsc && cd ../runner && tsc"
    }
}