{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "nohoist": [
    "api"
  ],
  "scripts": {
    "build": "cd ./packages/shared && tsc && cd ../api && tsc && cd ../runner && tsc"
  },
  "dependencies": {}
}
